<div class="bs-docs-section">
  <div class="page-header">
    <h1>Highlight<small>Behavior.Highlight.js</small></h1>
  </div>
  <p>Highlights an element on load using <code>Fx.Tween</code>'s <code>.highlight</code> element method.</p>

  <h3>Example</h3>
  <p>Hit refresh to see the highlight again.</p>
  <%= render layout: "codeblock" do %>
<div class="box" data-behavior="Highlight">
  I highlight when the page loads.
</div>
  <% end %>

  <h2>Behavior Options</h2>
  <p>These options apply only to the <code>data-</code> tag invocation.</p>
  <div class="table-responsive">
    <table class="table table-striped table-bordered">
      <thead>
       <tr>
         <th style="width: 100px;">Name</th>
         <th style="width: 50px;">type</th>
         <th style="width: 50px;">default</th>
         <th>description</th>
       </tr>
      </thead>
      <tbody>
        <tr>
          <td>fxOptions</td>
          <td>object</td>
          <td><code>{ duration: 1000 }</code></td>
          <td>Options object passed to <code>Fx.Tween</code> (see docs below).</td>
        </tr>
        <tr>
          <td>start</td>
          <td>string</td>
          <td>'#ffff88'</td>
          <td>The color the highlight starts at.</td>
        </tr>
        <tr>
          <td>end</td>
          <td>string</td>
          <td>'transparent'</td>
          <td>The color the highlight ends at.</td>
        </tr>
      </tbody>
    </table>
  </div>
</div>

<%= render partial: "md_docs", locals: {
  docs: [
    "node_modules/mootools-core/Docs/Fx/Fx.Tween.md",
    "node_modules/mootools-core/Docs/Fx/Fx.md"
  ]
} %>
