<div class="bs-docs-section">
  <div class="page-header">
    <h1>SelectWithOther <small>Behavior.SelectWithOther.js</small></h1>
  </div>
  <p>Allows users to enter an "other" value for a select list.</p>

  <p><span class="label label-primary">Note</span> You need to update your controller to interpret the selection
    of the "other" option as an instruction to use the value in the text input.</p>

  <h3>Examples</h3>
  <%= render layout: "codeblock" do %>
<p>
  <select data-trigger="selectWithOther" data-selectwithother-options="
    'hideClass': 'hide',
    'target': '!div input'
  ">
    <option>first</option>
    <option>second</option>
    <option>other</option>
  </select>
</p>
<div class="form-group">
  <input type="text" name="html" value="" placeholder="..." class="form-control input-hg" style="display:none">
</div>
  <% end %>


  <h2>Behavior Options</h2>
  <p>These options apply only to the <code>data-</code> tag invocation.</p>
  <div class="table-responsive">
    <table class="table table-striped table-bordered">
      <thead>
       <tr>
         <th style="width: 100px;">Name</th>
         <th style="width: 50px;">type</th>
         <th style="width: 50px;">default</th>
         <th>description</th>
       </tr>
      </thead>
      <tbody>
        <tr>
          <td>otherValue</td>
          <td>string</td>
          <td>'other'</td>
          <td>The value of the "other" option.</td>
        </tr>
        <tr>
          <td>target</td>
          <td>string</td>
          <td>~</td>
          <td>Selector to find the element to show when the "other" option is selected.</td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
